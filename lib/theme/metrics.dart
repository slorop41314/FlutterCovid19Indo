import 'package:flutter/material.dart';

const padding_container = EdgeInsets.all(10);
const padding_child = EdgeInsets.all(10);
const padding_horizontal = EdgeInsets.symmetric(horizontal: 20);
const padding_child_vertical = EdgeInsets.symmetric(vertical: 10);

const margin_child = EdgeInsets.all(10);
const margin_child_vertical = EdgeInsets.symmetric(vertical: 10);
const margin_horizontal = EdgeInsets.symmetric(horizontal: 20);
